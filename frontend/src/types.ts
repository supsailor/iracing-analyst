export type Session={id:string;created_at:string;track:string;car:string;source:string;laps:number;valid_laps:number;best_time:number|null;status:string;session_type:string;layout:string}
export type Lap={number:number;time:number;valid:boolean;representative:boolean;reason:string|null}
export type Evidence={metric:string;value:number;unit:string;comparison?:number|null}
export type Segment={id:string;name:string;start_pct:number;end_pct:number;confidence:number;best_time:number;median_time:number;selected_time:number;delta_to_best:number;stability:number;source_lap:number;entry_speed_kph:number;minimum_speed_kph:number;exit_speed_kph:number;steering_corrections:number}
export type Recommendation={rule:string;segment_id:string;title_key:string;message_key:string;confidence:number;expected_gain:number;evidence:Evidence[]}
export type Insight={id:string;kind:'positive'|'attention'|'warning';status:'confirmed'|'probable'|'insufficient';segment_id:string;channel:'speed'|'throttle'|'brake'|'steering'|'delta';distance_pct:number;title_key:string;message_key:string;time_delta:number;evidence:Evidence[]}
export type MapPoint={distance_pct:number;x:number;y:number}
export type TrackMarker={label:string;distance_pct:number;x:number;y:number;insight_id?:string|null}
export type TrackMap={available:boolean;source:'gps'|'integrated'|'unavailable';centerline:MapPoint[];best:MapPoint[];median:MapPoint[];corners:TrackMarker[];insights:TrackMarker[]}
export type DataSufficiency={status:'ready'|'limited'|'insufficient';valid_laps:number;required_laps:number;message_key:string}
export type Report={session_id:string;track:string;car:string;created_at:string;sample_count:number;laps:Lap[];best_lap:number|null;best_time:number|null;median_lap:number|null;median_time:number|null;sector_optimal:number|null;potential_gap:number|null;segments:Segment[];recommendations:Recommendation[];session_type:string;layout:string;track_id:number|null;official_turns:number|null;track_map:TrackMap;insights:Insight[];strengths:Insight[];improvement_actions:Recommendation[];data_sufficiency:DataSufficiency}
export type Series={distance_pct:number[];selected:Record<string,number[]>;reference:Record<string,number[]>}
